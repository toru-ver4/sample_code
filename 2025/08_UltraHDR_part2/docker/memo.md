```
docker build -t takuver4/ultrahdr:rev02 .
```

```
docker run -itd -P --name ultrahdr_rev02 -v C:\Users\toruv\OneDrive\work\sample_code:/work/src -v D:\abuse:/work/overuse --rm takuver4/ultrahdr:rev02
docker run -itd -P --name ultrahdr_rev02 -v /Users/toru/Work/sample_code:/work/src --rm takuver4/ultrahdr:rev02
```

```
docker-compose up -d
```
